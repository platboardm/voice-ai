{
  "stt": {
    "preservePrefix": "microphone.",
    "parameters": [
      {
        "key": "listen.model",
        "label": "Model",
        "type": "dropdown",
        "required": true,
        "default": "nova-3",
        "data": "speech-to-text-models.json",
        "valueField": "id",
        "errorMessage": "Please provide a valid deepgram model for speech to text."
      },
      {
        "key": "listen.language",
        "label": "Language",
        "type": "dropdown",
        "required": true,
        "default": "multi",
        "data": "speech-to-text-languages.json",
        "valueField": "code",
        "errorMessage": "Please provide a valid deepgram language for speech to text."
      },
      {
        "key": "listen.threshold",
        "label": "Transcript Confidence Threshold",
        "type": "slider",
        "required": false,
        "default": "0.5",
        "min": 0.1,
        "max": 0.9,
        "step": 0.1,
        "helpText": "Transcripts with a confidence score below this threshold will be filtered out."
      },
      {
        "key": "listen.keywords",
        "label": "Keyword / Keyterms",
        "type": "textarea",
        "required": false,
        "default": "",
        "colSpan": 2,
        "placeholder": "Enter keywords or key terms separated by space",
        "helpText": "Enter keywords separated by spaces. These will be used as key terms for transcription."
      }
    ]
  }
}
